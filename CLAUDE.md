@AGENTS.md

# data consistency.

always make sure the mongoose schema, rest api interface, validation fields , endpoint procedure, user interface and security setting of role based access, or public routes are correct and updated.

# spinner / loading animation

when it's loading, show a loading spinner so that users know that the system is running.

# for every geneated ai obejct

always set a createdAt and updatedAt timestamp of it
